# High-Resolution Interface Map

## Tree: `apps/console/src/hooks`

```
hooks/
├── use-mobile.ts
```

## File Summaries

### `use-mobile.ts`
**Role:** React hook that detects if the current viewport width corresponds to a mobile device based on a defined breakpoint.
**Key Exports:**
- `useIsMobile(): boolean` - Returns `true` if the window width is less than 768px.
**Dependencies:** `React`.