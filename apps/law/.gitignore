# --- Dependencies ---
node_modules/
pnpm-lock.yaml
# Keep package-lock.json or pnpm-lock.yaml, but usually not both if using pnpm. 
# Since you have pnpm-lock.yaml in your tree, you might want to ignore package-lock.json 
# if it was generated by mistake, otherwise keep both tracked.

# --- Secrets & Environment (CRITICAL) ---
.env
.env.local
.env.*.local
credentials.json
google_credentials.json
service_account.json

# --- Build Outputs ---
dist/
build/
out/

# --- Runtime Data & Logs ---
debug_output/
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Testing ---
coverage/

# --- System & IDE ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# --- Optional: Local Data ---
# If you don't want to commit the PDFs you downloaded:
# sample_docs/parsed/